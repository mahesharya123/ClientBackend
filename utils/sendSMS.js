const sendSMS = async (mobile, message) => {
  // Integrate with Twilio or another SMS API
  console.log(`Sending SMS to ${mobile}: ${message}`);
  // Example placeholder
};

module.exports = { sendSMS };